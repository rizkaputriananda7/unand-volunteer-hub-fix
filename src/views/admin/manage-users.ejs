<!DOCTYPE html>
<html lang="id">
<head>
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-100">
    <div class="flex h-screen">
        <%- include('../partials/sidebar_admin', { user: user, title: title }) %>
        <main class="flex-1 p-8 overflow-y-auto">
            <h1 class="text-3xl font-bold text-gray-800 mb-6">Kelola Akun Pengguna</h1>
            
            <div class="mb-6">
                <form action="/admin/users" method="GET">
                    <div class="relative">
                        <input type="text" name="q" value="<%= searchQuery %>" placeholder="Cari berdasarkan Nama atau NIM/NIP..." class="w-full p-3 pl-10 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-purple-500">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                        </div>
                    </div>
                </form>
            </div>

            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <table class="w-full text-left">
                    <thead class="bg-gray-50 border-b-2 border-gray-200">
                        <tr>
                            <th class="p-4 font-semibold">Nama</th>
                            <th class="p-4 font-semibold">NIM / NIP</th>
                            <th class="p-4 font-semibold">Peran</th>
                            <th class="p-4 font-semibold">Status</th>
                            <th class="p-4 font-semibold">Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(u => { %>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="p-4 font-medium text-gray-800"><%= u.nama %></td>
                                <td class="p-4 text-gray-600"><%= u.nim_nip %></td>
                                <td class="p-4 text-gray-600"><%= u.role %></td>
                                <td class="p-4">
                                    <span class="px-3 py-1 text-sm font-semibold rounded-full <%= u.status === 'Aktif' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                                        <%= u.status %>
                                    </span>
                                </td>
                                <td class="p-4">
                                    <a href="#" class="text-purple-600 hover:text-purple-800 font-semibold">Edit</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>