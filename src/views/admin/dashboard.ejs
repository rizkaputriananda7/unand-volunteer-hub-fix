<!DOCTYPE html>
<html lang="id">
<head>
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-100">
    <div class="flex h-screen">
        <%- include('../partials/sidebar_admin', { user: user, title: title }) %>
        <main class="flex-1 p-8 overflow-y-auto">
            <h1 class="text-3xl font-bold text-gray-800">Dashboard Administrasi</h1>
            <p class="text-gray-600 mt-2">Selamat datang, <%= user.name %>! Berikut adalah ringkasan sistem.</p>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mt-8">
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <h3 class="text-gray-500 text-sm font-medium">Total Pengguna</h3>
                    <p class="text-3xl font-bold text-gray-800 mt-2"><%= stats.totalUsers %></p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <h3 class="text-gray-500 text-sm font-medium">Total Pusat Volunteer</h3>
                    <p class="text-3xl font-bold text-gray-800 mt-2"><%= stats.totalPusat %></p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <h3 class="text-gray-500 text-sm font-medium">Total Program</h3>
                    <p class="text-3xl font-bold text-gray-800 mt-2"><%= stats.totalProgram %></p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <h3 class="text-gray-500 text-sm font-medium">Total Pengumuman</h3>
                    <p class="text-3xl font-bold text-gray-800 mt-2"><%= stats.totalPengumuman %></p>
                </div>
            </div>

            <div class="mt-10">
                <h2 class="text-xl font-semibold text-gray-700 mb-4">Aksi Cepat</h2>
                <div class="bg-white p-6 rounded-lg shadow-sm">
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <a href="#" class="bg-purple-600 text-white text-center font-bold py-3 px-4 rounded-lg hover:bg-purple-700 transition-colors">
                            Kelola Akun Pengguna
                        </a>
                        <a href="#" class="bg-purple-600 text-white text-center font-bold py-3 px-4 rounded-lg hover:bg-purple-700 transition-colors">
                            Kelola Pusat Volunteer
                        </a>
                        <a href="#" class="bg-purple-600 text-white text-center font-bold py-3 px-4 rounded-lg hover:bg-purple-700 transition-colors">
                            Buat Pengumuman Sistem
                        </a>
                    </div>
                </div>
            </div>

        </main>
    </div>
</body>
</html>