<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50">
    <div class="flex h-screen">
        <aside class="w-64 bg-white shadow-md flex flex-col">
            <div class="p-6 flex items-center space-x-3">
                <div class="bg-teal-500 p-2 rounded-lg"><svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg></div>
                <span class="text-xl font-bold text-gray-800">Unand Volunteer Hub</span>
            </div>
            <nav class="flex-1 px-4 py-2 space-y-2">
                <a href="/mahasiswa/dashboard" class="flex items-center px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">Dashboard</a>
                <a href="#" class="flex items-center px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">Program Volunteer</a>
                <a href="/mahasiswa/pengumuman" class="flex items-center px-4 py-2 text-white bg-teal-500 rounded-lg">Pengumuman</a>
                <a href="#" class="flex items-center px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">Penjadwalan</a>
                <a href="#" class="flex items-center px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">Riwayat Keikutsertaan</a>
                <a href="/mahasiswa/faq" class="flex items-center px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-lg">FAQ</a>
            </nav>
            <div class="p-4 border-t">
                <div class="flex items-center space-x-3 mb-4">
                    <div class="w-10 h-10 rounded-full bg-indigo-200 text-indigo-700 flex items-center justify-center font-bold">NH</div>
                    <div>
                        <p class="font-semibold text-gray-800"><%= user.name %></p>
                        <p class="text-sm text-gray-500"><%= user.role %></p>
                    </div>
                </div>
                <a href="/" class="flex items-center justify-center w-full px-4 py-2 text-red-600 hover:bg-red-50 rounded-lg">Logout</a>
            </div>
        </aside>

        <main class="flex-1 p-8 overflow-y-auto">
            <div class="max-w-4xl mx-auto">
                <a href="/mahasiswa/pengumuman" class="text-teal-600 font-semibold mb-6 inline-block">&larr; Kembali ke Daftar Pengumuman</a>
                <div class="bg-white p-8 rounded-lg shadow-sm">
                    <p class="text-sm text-gray-500 mb-2"><%= announcement.tanggal %></p>
                    <h1 class="text-3xl font-bold text-gray-900 mb-4"><%= announcement.judul %></h1>
                    <div class="prose max-w-none text-gray-700">
                        <p><%= announcement.isi %></p>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>