<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50">
    <div class="flex h-screen">

        <%- include('../partials/sidebar_mahasiswa', { user: user, title: title }) %>

        <main class="flex-1 p-8 overflow-y-auto">
            <div class="flex justify-between items-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800">Dashboard</h1>
                <div class="w-10 h-10 rounded-full bg-gray-200"></div>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-sm mb-8">
                <h2 class="text-2xl font-semibold text-gray-800">Selamat Datang, <%= user.name.split(' ')[0] %>!</h2>
                <p class="text-gray-500">Berikut adalah ringkasan aktivitas volunteer Anda.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                <div class="bg-white p-6 rounded-lg shadow-sm flex items-center space-x-4">
                    <div class="bg-green-100 p-3 rounded-full"><span class="text-2xl">🎉</span></div>
                    <div>
                        <p class="text-gray-500">Program Aktif</p>
                        <p class="text-2xl font-bold text-gray-800"><%= aktif %></p>
                    </div>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm flex items-center space-x-4">
                    <div class="bg-orange-100 p-3 rounded-full"><span class="text-2xl">⏳</span></div>
                    <div>
                        <p class="text-gray-500">Pendaftaran Diproses</p>
                        <p class="text-2xl font-bold text-gray-800"><%= diproses %></p>
                    </div>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-sm flex items-center space-x-4">
                    <div class="bg-blue-100 p-3 rounded-full"><span class="text-2xl">🔔</span></div>
                    <div>
                        <p class="text-gray-500">Pengumuman Baru</p>
                        <p class="text-2xl font-bold text-gray-800"><%= pengumumanBaru %></p>
                    </div>
                </div>
                <a href="/mahasiswa/bookmark" class="block bg-white p-6 rounded-lg shadow-sm hover:shadow-lg transition-shadow">
                    <div class="flex items-center space-x-4">
                        <div class="bg-purple-100 p-3 rounded-full"><span class="text-2xl">⭐</span></div>
                        <div>
                            <p class="text-gray-500">Program Favorit</p>
                            <p class="text-lg font-bold text-gray-800">Lihat Daftar</p>
                        </div>
                    </div>
                </a>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-sm">
                <h3 class="text-xl font-semibold text-gray-800 mb-4">Jadwal Mendatang</h3>
                <div class="space-y-4">
                    <% jadwalMendatang.forEach(jadwal => { %>
                    <div class="flex justify-between items-center p-4 rounded-lg hover:bg-gray-50">
                        <div>
                            <p class="font-semibold text-gray-800"><%= jadwal.judul %></p>
                            <p class="text-sm text-gray-500"><%= jadwal.waktu %> - <%= jadwal.tempat %></p>
                        </div>
                        <a href="<%= jadwal.link %>" class="text-teal-600 font-semibold">Lihat Detail</a>
                    </div>
                    <% }) %>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
