<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50">
    <div class="flex h-screen">
        
        <%- include('../partials/sidebar_mahasiswa', { user: user, title: title }) %>

        <main class="flex-1 p-8 overflow-y-auto">
            <div class="flex justify-between items-center mb-8">
                <h1 class="text-3xl font-bold text-gray-800">Pengumuman</h1>
                <div class="w-10 h-10 rounded-full bg-gray-200"></div>
            </div>

            <div class="space-y-6">
                <% pengumuman.forEach(p => { %>
                    <div class="bg-white p-6 rounded-lg shadow-sm border border-gray-200">
                        <p class="text-sm text-gray-500 mb-2"><%= p.tanggal %></p>
                        <h2 class="text-xl font-bold text-gray-800 mb-3"><%= p.judul %></h2>
                        <p class="text-gray-600 mb-4"><%= p.isi %></p>
                        <a href="/mahasiswa/pengumuman/<%= p.id %>" class="font-semibold text-teal-600 hover:text-teal-700">Baca Selengkapnya</a>
                    </div>
                <% }) %>
                </div>
        </main>
    </div>
</body>
</html>
