<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Program Volunteer - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50">
    <div class="flex min-h-screen">
        <%- include('../partials/sidebar_mahasiswa', { user: user, title: 'Detail Program' }) %>
        <main class="flex-1 p-8 overflow-y-auto">
            <div class="max-w-2xl mx-auto bg-white rounded-xl shadow p-8">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800 mb-1"><%= program.nama %></h1>
                <p class="text-gray-500 mb-4">Penyelenggara: <span class="font-semibold text-teal-600"><%= program.penyelenggara %></span></p>
                <div class="mb-6">
                    <div class="w-full h-56 md:h-64 bg-teal-100 flex items-center justify-center rounded-lg mb-4">
                        <span class="text-xl text-teal-500">Gambar Program Migas</span>
                    </div>
                </div>
                <h2 class="text-lg font-semibold text-gray-800 mb-2">Deskripsi Program</h2>
                <p class="text-gray-700 mb-6"><%= program.deskripsi %></p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                    <div>
                        <h3 class="text-base font-semibold text-gray-800 mb-2">Persyaratan</h3>
                        <ul class="list-disc list-inside text-gray-700 space-y-1">
                            <% program.persyaratan.forEach(syarat => { %>
                                <li><%= syarat %></li>
                            <% }) %>
                        </ul>
                    </div>
                    <div>
                        <h3 class="text-base font-semibold text-gray-800 mb-2">Manfaat</h3>
                        <ul class="list-disc list-inside text-gray-700 space-y-1">
                            <% program.manfaat.forEach(mnf => { %>
                                <li><%= mnf %></li>
                            <% }) %>
                        </ul>
                    </div>
                </div>
                <div class="flex flex-wrap gap-4 mb-4">
                    <div class="flex items-center text-gray-700 text-sm bg-gray-100 rounded px-3 py-1">
                        <span class="font-semibold mr-1">Kuota:</span> <%= program.kuota || '-' %> Peserta
                    </div>
                    <div class="flex items-center text-gray-700 text-sm bg-gray-100 rounded px-3 py-1">
                        <span class="font-semibold mr-1">Durasi:</span> <%= program.durasi %>
                    </div>
                    <div class="flex items-center text-gray-700 text-sm bg-gray-100 rounded px-3 py-1">
                        <span class="font-semibold mr-1">Periode Pendaftaran:</span> <%= program.periode || '-' %>
                    </div>
                </div>
                <div class="flex flex-wrap gap-4 mb-6">
                    <div class="flex items-center text-gray-700 text-sm bg-gray-100 rounded px-3 py-1">
                        <span class="font-semibold mr-1">Lokasi:</span> <%= program.lokasi || '-' %>
                    </div>
                </div>
                <div class="mb-6">
                    <h3 class="text-base font-semibold text-gray-800 mb-2">Kontak Koordinator</h3>
                    <div class="text-gray-700 text-sm">
                        <div><span class="font-semibold">Nama:</span> <%= program.kontak.nama %></div>
                        <div><span class="font-semibold">WhatsApp:</span> <a href="https://wa.me/<%= program.kontak.wa %>" class="text-teal-600 hover:underline" target="_blank"><%= program.kontak.wa %></a></div>
                        <% if(program.kontak.email) { %>
                        <div><span class="font-semibold">Email:</span> <a href="mailto:<%= program.kontak.email %>" class="text-teal-600 hover:underline"><%= program.kontak.email %></a></div>
                        <% } %>
                    </div>
                </div>
                <div class="flex flex-col md:flex-row gap-3 mt-6">
                    <a href="/mahasiswa/daftar/<%= program._id %>" class="flex-1 bg-teal-600 hover:bg-teal-700 text-white font-semibold px-6 py-2 rounded-lg transition text-center">Daftar Program Ini</a>
                    <form action="/mahasiswa/bookmark" method="post">
                        <input type="hidden" name="programId" value="<%= program._id %>">
                        <button type="submit" class="flex-1 bg-white border border-teal-600 text-teal-600 font-semibold px-6 py-2 rounded-lg transition hover:bg-teal-50">Bookmark Program</button>
                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
