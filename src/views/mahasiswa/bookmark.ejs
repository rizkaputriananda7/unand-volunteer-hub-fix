<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Unand Volunteer Hub</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-teal-50/50">
    <div class="flex h-screen">
        
        <%- include('../partials/sidebar_mahasiswa', { user: user, title: title }) %>

        <main class="flex-1 p-8 overflow-y-auto">
            <h1 class="text-3xl font-bold text-gray-800 mb-8">Program Favorit Tersimpan</h1>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% if (programs.length > 0) { %>
                    <% programs.forEach(p => { %>
                        <div class="bg-white p-6 rounded-xl shadow-md border border-gray-200 flex flex-col justify-between relative">
                            <!-- Icon bookmark di kanan atas -->
                            <form action="/bookmark/<%= p.id %>/delete" method="POST" onsubmit="return confirm('Hapus program dari bookmark?')" class="absolute top-4 right-4 z-10">
                                <button type="submit" title="Hapus dari bookmark" class="bg-white rounded-full shadow p-1 border border-gray-200 hover:bg-red-50 focus:outline-none">
                                    <svg class="w-7 h-7 text-red-500 hover:text-red-700 transition" fill="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16c0 .8-.8 1.3-1.5.9L12 18.5l-5.5 3.4c-.7.4-1.5-.1-1.5-.9V5z"></path></svg>
                                </button>
                            </form>
                            <div>
                                <h2 class="text-2xl font-bold text-gray-800 mb-2"><%= p.judul %></h2>
                                <p class="text-base text-gray-600 mb-2">Pusat: <span class="font-medium"><%= p.pusat %></span> | Kuota: <%= p.kuota %> | Durasi: <%= p.durasi %></p>
                            </div>
                            <div class="flex justify-end mt-4">
                                <a href="/mahasiswa/program/<%= p.id %>" class="bg-teal-500 text-white font-semibold py-2 px-5 rounded-md hover:bg-teal-600 transition">Lihat & Daftar</a>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-gray-500 col-span-full">Anda belum menyimpan program apa pun. Kembali ke halaman <a href="/mahasiswa/program" class="text-teal-600 font-semibold">Program Volunteer</a> untuk menyimpan.</p>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>
