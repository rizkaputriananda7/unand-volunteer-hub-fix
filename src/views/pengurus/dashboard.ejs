<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/tailwind.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-8">
        <h1 class="text-3xl font-bold mb-2 text-gray-800">Dashboard Pengurus</h1>
        <p class="text-gray-600 mb-6">Selamat datang, <%= user.name %>!</p>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-xl font-semibold text-gray-700">Total Program Anda</h2>
                <p class="text-4xl font-bold text-blue-600 mt-2"><%= stats.totalPrograms %></p>
            </div>
            <div class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-xl font-semibold text-gray-700">Total Pendaftar</h2>
                <p class="text-4xl font-bold text-green-600 mt-2"><%= stats.totalApplicants %></p>
            </div>
             <div class="bg-white p-6 rounded-lg shadow-md flex flex-col justify-center items-center">
                <a href="/pengurus/faq" class="w-full text-center bg-indigo-600 text-white font-bold py-3 px-4 rounded-lg hover:bg-indigo-700 transition-colors">
                    Kelola Konten FAQ
                </a>
            </div>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-2xl font-bold">Program yang Anda Kelola</h2>
                <a href="/pengurus/program/create" class="bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700">
                    + Buat Program Baru
                </a>
            </div>
            <div class="overflow-x-auto">
                <table class="min-w-full bg-white">
                    <thead class="bg-gray-200">
                        <tr>
                            <th class="py-2 px-4 text-left">Nama Program</th>
                            <th class="py-2 px-4 text-left">Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (programs.length > 0) { %>
                            <% programs.forEach(program => { %>
                                <tr class="border-b">
                                    <td class="py-2 px-4 font-semibold"><%= program.title %></td>
                                    <td class="py-2 px-4 space-x-4">
                                        <a href="/pengurus/program/<%= program.id %>/edit" class="text-blue-600 hover:underline font-semibold">Edit</a>
                                        <a href="/pengurus/program/<%= program.id %>/jadwal" class="text-green-600 hover:underline font-semibold">Kelola Jadwal</a>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="2" class="py-4 px-4 text-center text-gray-500">Anda belum membuat program apapun.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>